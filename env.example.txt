# ===== OpenDeepSearch Configuration =====
# API keys for web search and content retrieval
SERPER_API_KEY=your-serper-api-key-here          # Serper.dev API key (for Google search)
JINA_API_KEY=your-jina-api-key-here              # Jina.ai API key (for search result reranking)
OPENROUTER_API_KEY=your-openrouter-api-key-here  # OpenRouter API key (for accessing various LLM models)

# Optional: Use SearXNG as search engine (self-hosted alternative)
# SEARXNG_INSTANCE_URL=https://your-searxng-instance.com
# SEARXNG_API_KEY=your-api-key-here

# ===== LLM Model Configuration =====
# Primary language model for prediction analysis
LITELLM_MODEL_ID=openrouter/google/gemini-2.0-flash-001  # LiteLLM format model ID

# ===== OpenRouter Configuration =====
# OpenRouter is a unified LLM API gateway supporting multiple models
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1           # OpenRouter API base URL
OPENROUTER_MODEL=google/gemini-2.0-flash-001                # Model name to use
YOUR_SITE_URL=https://your-site.com                         # Optional: Your site URL (for OpenRouter usage stats)
YOUR_SITE_NAME=Prediction AI Agent                          # Optional: Your site name (for OpenRouter ranking display)

# ===== Search Configuration =====
SEARCH_PROVIDER=serper      # Search provider: serper (recommended) or searxng (self-hosted)
RERANKER=jina              # Search result reranker: jina (recommended) or infinity

# ===== API Service Configuration =====
API_HOST=0.0.0.0           # API service listen address (0.0.0.0 means all network interfaces)
API_PORT=8000              # API service port number
DEBUG=True                 # Debug mode (recommended False for production)

# ===== Prediction System Configuration =====
MAX_SEARCH_RESULTS=10                    # Maximum number of results per search query
PREDICTION_CONFIDENCE_THRESHOLD=0.6      # Prediction confidence threshold (warning if below)
KELLY_FRACTION=0.25                      # Kelly criterion fraction (for betting advice, range 0-1)
MAX_BET_PERCENTAGE=0.05                  # Maximum single bet percentage (5% of bankroll)

# ===== Logging Configuration =====
LOG_LEVEL=INFO              # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=logs/agent.log     # Log file save path

